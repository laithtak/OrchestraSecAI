# OrchestraSecAI Check SDK (stub)

Phase 2+ package for third-party plugin authors. MVP plugins use `apps/api/src/orchestrasecai/checks/base.py` directly.

Planned exports:

- `SecurityCheck` ABC
- `FindingDraft`, `CheckContext`, `CheckPhase`
- Entry point group `orchestrasecai.checks`
